{{- if .Values.sequi.front.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "seq-ui.fullname" . }}
  labels:
    {{- include "seq-ui.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "seq-ui.fullname" . }}
  minReplicas: {{ .Values.sequi.front.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.sequi.front.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.sequi.front.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.sequi.front.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.sequi.front.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.sequi.front.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
