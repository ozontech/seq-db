{{- if .Values.sequi.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "seq-ui.fullname" . }}
  labels:
    {{- include "seq-ui.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "seq-ui.fullname" . }}
  minReplicas: {{ .Values.sequi.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.sequi.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.sequi.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.sequi.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.sequi.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.sequi.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
