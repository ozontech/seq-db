{{- if .Values.seqproxy.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "seq-proxy.fullname" . }}
  labels:
    {{- include "seq-proxy.labels" . | nindent 4 }}
  {{- with .Values.seqproxy.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.seqproxy.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.seqproxy.ingress.tls }}
  tls:
    {{- range .Values.seqproxy.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.seqproxy.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "seq-proxy.fullname" $ }}
                port:
                  number: {{ $.Values.seqproxy.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
